// 1.	Leer un número entero y determinar si tiene 3 dígitos

function cantidades_de_digito(){
    var numero = 155
    var convertir_numero = numero.toString()
        if(convertir_numero.length == 3) {

            console.log("la variable consta de 3 digitos")
        
        }else { console.log("la variable no tiene 3 digitos")

        }

    }

cantidades_de_digito()